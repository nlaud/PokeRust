# TODO
### Fixes
- `random_doubles_battles_are_sound` (poke_rust/src/tests/random_battle_tests.rs): fixed four real Pass-3/BCP oracle bugs (Mold Breaker unmodeled in contact-chip absence, Last Respects' fainted-teammate count invisible to the oracle's empty-bench skeleton, spread-move ×0.75 using a format-only heuristic instead of the hit's real resolved target count, and the identical crossed-bound gap in `SpeedComparison` propagation), added self-heal safety nets for remaining mathematically-provable over-narrowing, and regenerated `pokemon_info/showdownLearnsets.txt` from the Champions mod's own learnset source (`helper_scripts/gen_learnsets.py`, now with prevo-chain move inheritance). Later fixed the Illusion re-disguise gap (Zoroark re-arms on switch-out via `rearm_zoroark_on_side`), a P1-viewer-gate bug in `pass3_direction_a` (S43), status mutual-exclusivity from silent status clears with no emitted event (S44/S45: Frozen-thaw, Shed Skin/Hydration/Healer), three more silent-status-clear sites (Natural Cure on switch-out, Sparkling Aria's Burn cure, Uproar's wake — both call sites), a sand-immunity absence-check that treated weather's naturally-expiring last turn as proof of immunity, S46 (Direction-B damage oracle required an exact damage match even for lethal/KO hits, wrongly excluding any attacker stat strong enough to overkill), and S49 (a benched Pokémon's `mon_idx` is a live Vec-position, not a stable id — an Illusion promotion's bench-restore push could net-grow one side's back bucket with no offsetting removal, silently shifting every index in the other side's back segment and letting a stale CNF predicate get force-committed onto the wrong mon). In the latest session: Rest/Worry Seed silently overwriting/curing a prior status with no `StatusCured` emitted; `pass2_powder_immunity` accepting `MoveFailed`/`Blocked` (not just `Immune`) as sand/powder-immunity evidence, over-narrowing an item to `Known` too early; S47 (Direction A had the identical lethal-hit bug S46 fixed for Direction B — a fainting hit's `exact_damage` is a lower bound, not exact, and the inverted-monotonicity direction meant the defender's *minimum* stat bound got wrongly raised); S48 (Payback's oracle BP always assumed the target had already acted, since `materialize_battle`'s synthetic skeleton always has an empty `action_queue` — resolved from `ctx.move_users_this_turn`, populated live from the real event stream, instead); King's Rock/Razor Fang flinch attribution now requires the attacker's slot to have had no switch this turn (a switch there makes `mon_idx_for_active_slot`'s re-resolution untrustworthy — attribution isn't provably sound, so no clause is pushed); and `pass1_switch`'s/`bench_outgoing_mon`'s S37 "already placed" shortcut is now gated on `turn_number == 0` — it's meant to fire only at the one-time team-preview→battle transition, but a mid-battle switch can coincidentally match the slot's own prior occupant's species under Illusion (a disguised Zoroark), and the shortcut was skipping `maybe_resolve_illusion_two_in_front`'s two-in-front refinement entirely whenever that coincidence happened. Full test suite passes (1309/1309). **This is a confirmed multi-cause investigation** — each bug family below has been hit, fixed once, and found to still recur via a different, not-yet-found cause on a fresh sweep. See memory `project_random_battle_fuzz_test_findings.md` for the full trail, the diagnostic methodology that worked (parallel eprintln at the panic site + ground-truth dump, cross-referenced by species), and traps hit along the way (self-heals that turned out to be load-bearing promotion signals). Currently open, not yet root-caused:
  - **pass5 "every candidate nature is infeasible" panic** — still recurs after S43 and S46 each fixed one real cause. At least one more cause remains (confirmed via a 15-run sweep, 5/15 still hit it via other triggers).
  - **Mega-stone/item Known-conflict** (`Known(SafetyGoggles) vs new Known(Floettite)` on `MegaEvolution`) — still recurs after fixing the sand-immunity weather-expiry cause and the `pass2_powder_immunity` over-acceptance. A further absence-inference path may still force an item to `Known` too early — not re-confirmed this session, re-check on a fresh sweep.
  - **A third status-conflict pair** (Poison↔Paralysis, Poison↔Sleep, Burn↔Paralysis, Burn↔Frozen all observed) — the `turn_number` gate above fixed one confirmed cause (repeated same-slot `already_placed` misfires), but a 3000-iteration sweep still reproduced a Burn→Paralysis and a Burn→Frozen conflict afterward, on a mon with NO switch or status event of any kind recorded for its mon_idx before the panic (ruled out via targeted per-mon_idx event tracing) — meaning either a status-infliction path exists that bypasses the central `mon.status.is_some()` gate in `simulator/helpers.rs` (audited ~25 known status-mutation sites, all safe), or a still-unidentified silent cure. Not yet root-caused; needs fresh tracing.
  - **Doubles same-species Illusion-disguise collision, cross-slot case** (root-caused by S49's fix commit, not yet fixed) — `pass1_switch`'s bench-matching, when a switch's incoming species matches a DIFFERENT active slot's species on the same side (not the same slot re-used, which the `turn_number` gate above now handles), may still mismatch attribution. `maybe_resolve_illusion_two_in_front` covers the case where the arriving mon itself needs a fresh hypothesis, but does not correct the ALREADY-active duplicate's own tracking. Needs dedicated tracing.
### New features
- Link all the READMEs to the main project README.
- Frontend features
  - We need a favicon lul
- Eventually create nash solver and recursive evaluation (When both players have perfect information)
- Create a meta sampler from pikalytics, and then get the algorithm to understand that
  - [ ] Tracker page: needs a parser for lines of input -> action / reaction tree
        (figuring out what is a reaction to what and what causes what from just the
        lines, also must add guaranteed effects so the user doesn't need to put those
        in manually). This will likely include some RegEx stuff as well. Need a
        detailed spec.
    - https://stitch.withgoogle.com/projects/6512361286860616575 for designs
    - https://github.com/PokeAPI/sprites for FE sprites (fetched at runtime, never committed)
  - [ ] Then move on to actual bot creation, battle and mentor pages.
