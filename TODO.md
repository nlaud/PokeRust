# TODO
### Fixes
- `random_doubles_battles_are_sound` (poke_rust/src/tests/random_battle_tests.rs): fixed four real Pass-3/BCP oracle bugs (Mold Breaker unmodeled in contact-chip absence, Last Respects' fainted-teammate count invisible to the oracle's empty-bench skeleton, spread-move ×0.75 using a format-only heuristic instead of the hit's real resolved target count, and the identical crossed-bound gap in `SpeedComparison` propagation), added self-heal safety nets for remaining mathematically-provable over-narrowing, and regenerated `pokemon_info/showdownLearnsets.txt` from the Champions mod's own learnset source (`helper_scripts/gen_learnsets.py`, now with prevo-chain move inheritance). Later fixed the Illusion re-disguise gap (Zoroark re-arms on switch-out via `rearm_zoroark_on_side`), a P1-viewer-gate bug in `pass3_direction_a` (S43), status mutual-exclusivity from silent status clears with no emitted event (S44/S45: Frozen-thaw, Shed Skin/Hydration/Healer), three more silent-status-clear sites (Natural Cure on switch-out, Sparkling Aria's Burn cure, Uproar's wake — both call sites), a sand-immunity absence-check that treated weather's naturally-expiring last turn as proof of immunity, S46 (Direction-B damage oracle required an exact damage match even for lethal/KO hits, wrongly excluding any attacker stat strong enough to overkill), and S49 (a benched Pokémon's `mon_idx` is a live Vec-position, not a stable id — an Illusion promotion's bench-restore push could net-grow one side's back bucket with no offsetting removal, silently shifting every index in the other side's back segment and letting a stale CNF predicate get force-committed onto the wrong mon). In the latest session: Rest/Worry Seed silently overwriting/curing a prior status with no `StatusCured` emitted; `pass2_powder_immunity` accepting `MoveFailed`/`Blocked` (not just `Immune`) as sand/powder-immunity evidence, over-narrowing an item to `Known` too early; S47 (Direction A had the identical lethal-hit bug S46 fixed for Direction B — a fainting hit's `exact_damage` is a lower bound, not exact, and the inverted-monotonicity direction meant the defender's *minimum* stat bound got wrongly raised); S48 (Payback's oracle BP always assumed the target had already acted, since `materialize_battle`'s synthetic skeleton always has an empty `action_queue` — resolved from `ctx.move_users_this_turn`, populated live from the real event stream, instead); King's Rock/Razor Fang flinch attribution now requires the attacker's slot to have had no switch this turn (a switch there makes `mon_idx_for_active_slot`'s re-resolution untrustworthy — attribution isn't provably sound, so no clause is pushed); and `pass1_switch`'s/`bench_outgoing_mon`'s S37 "already placed" shortcut is now gated on `turn_number == 0` — it's meant to fire only at the one-time team-preview→battle transition, but a mid-battle switch can coincidentally match the slot's own prior occupant's species under Illusion (a disguised Zoroark), and the shortcut was skipping `maybe_resolve_illusion_two_in_front`'s two-in-front refinement entirely whenever that coincidence happened. A further session fixed S50 (the cross-slot Illusion-disguise collision) and S51 (faint silently clearing status with no matching event). The next session fixed S52a (a stale species-derived CNF predicate surviving an Illusion promotion and getting force-applied against the newly-promoted mon's real stat bounds) and S52b (consuming a hypothesis-carrying bench entry with no eager placeholder restore, letting a later genuine sighting of the real decoy fabricate a phantom duplicate) — one more confirmed, fixed, tested cause each for the pass5-nature-infeasible and item-Known-conflict families respectively. Both families remain open: pass5-nature recurred once post-fix on non-Illusion Archaludon; item-Known-conflict recurred on P1's non-Illusion side, confirming the family has (at least) two independent causes — the roster-duplication one just fixed, and a still-open absence-inference one. Full test suite passes (1314/1314). **This is a confirmed multi-cause investigation** — each bug family below has been hit, fixed once, and found to still recur via a different, not-yet-found cause on a fresh sweep. See memory `project_random_battle_fuzz_test_findings.md` for the full trail, the diagnostic methodology that worked (parallel eprintln at the panic site + ground-truth dump, cross-referenced by species), and traps hit along the way (self-heals that turned out to be load-bearing promotion signals). Currently open, not yet root-caused:
  - **pass5 "every candidate nature is infeasible" panic — one more real cause fixed (S52a), family still open**. `pass4_speed_from_order` was investigated and **ruled out** as a cause (the simulator's actual speed function applies exactly what pass4 folds/escapes — see prior entry, unchanged). The actually-confirmed cause this session (S52a): `apply_with_illusion_mirroring` returning `Promoted` wholesale-replaces the mon's `UnknownPokemonState` fields via `promote_illusion_to_primary`, but that struct swap never touched the separate, mon_idx-keyed `state.predicates` CNF store — a `SpeedComparison`/`EVIVStatGE`/`EVIVStatLE` clause recorded against this mon_idx **before** promotion (derived under the WRONG, decoy-species assumption) survived and got force-applied against the NEW, post-promotion stat bounds, producing an unsatisfiable window many events later. **Fix**: `purge_species_derived_predicates_for_mon` (`inference.rs`) — reuses the SAME species-derived-only filter (`statement_stale_after_species_reveal`) the dedicated `IllusionEnded` handler already used for its own direct-reveal case — now called at all four `apply_with_illusion_mirroring` call sites that can return `Promoted` (Pass 1 move/item reveals ×3, Pass 5's own backstop), not just `IllusionEnded`. Regression: `test_s52_illusion_promotion_purges_stale_species_derived_predicate` (`inference_tests.rs`) — verified red without the fix (a stale `EVIVStatGE{Atk≥110}` injected pre-promotion landed straight in the promoted Zoroark's own `min_pre_nature_stat[1]`). Full suite green (1313/1313); 8×25/6×500-iteration sweeps found zero recurrences of *this* mechanism.

    **Still open**: a fresh 500-iteration sweep (post-fix) still hit this panic once, on **Archaludon** (non-Illusion, unrelated to S52a) — `every candidate nature is infeasible for mon_idx=1 stat 1 (minStat=164, maxStat=176)`, preceded by a self-heal on the SAME window for a DIFFERENT mon_idx earlier that battle. The trace instrumentation used to find S52a was removed after landing the fix (per this project's "temporary, not committed" convention); re-adding it at `apply_unconditional_tightening`'s tightening call (inference.rs) and BCP's `EVIVStatGE`/`EVIVStatLE` success branches (bcp.rs), filtered to stat index 1, is the fastest way to re-catch this specific instance with full write history.
  - **Mega-stone/item Known-conflict family — one more real cause fixed (S52b), family still open**. Deep-traced last session's `Not([Leftovers]) excludes the revealed value Leftovers` repro to its root mechanism (confirmed via a live fuzz-test run + targeted code trace, not static guessing): a disguised Zoroark lead shows the species of **the last non-fainted bench member** (`compute_illusion_disguise`, real game mechanics) — `pass1_switch` correctly (by existing design, per a passing test) consumes that decoy's OWN `possible_back` entry into the active slot to represent the "could genuinely be the decoy, or could be Zoroark" ambiguity as one entry. The gap: consuming it removes the ONLY placeholder that could ever represent "the real decoy, if this active slot turns out to actually be Zoroark" — the EXISTING promotion-time restore (`finish_illusion_promotion_restore`/`restore_discarded_primary_to_bench`) only fires reactively, at promotion, and a later genuine switch-in of the real decoy (while the disguise is still unresolved, no longer concurrently active — outside S50's cross-slot-collision coverage) found nothing on the bench and fabricated a phantom duplicate. Confirmed via a live trace: a `Known(ZoroarkHisui)` slot getting fed a decoy's move (e.g. Garchomp's DragonClaw) — proof two independently-tracked entries claimed the same physical mon.

    **Fix**: `pass1_switch` now eagerly restores a hypothesis-less placeholder (via `restore_discarded_primary_to_bench`, extended with a `seed_hypothesis: bool` parameter) the moment it consumes a bench entry that itself carries a live Zoroark hypothesis — symmetric to what promotion-time restore already does lazily. **Trap hit and fixed while building this**: `restore_discarded_primary_to_bench` unconditionally calls `maybe_seed_fresh_hypothesis`, whose own doc comment assumes it only ever runs POST-resolution (`unresolved_zoroark_count` already 0) — calling it as-is from `pass1_switch` (which runs BEFORE resolution) re-seeded a hypothesis onto the new placeholder too, letting it and the active slot's own hypothesis both independently chase promotion for the same one real Zoroark (the exact double-promotion class this whole family keeps producing). Caught by the regression test itself (first version asserted `possible_illusion_state.is_none()` and failed even with the "fix" active) before it could reach a sweep. `seed_hypothesis=false` at the new call site; unchanged (`true`) at the two existing post-resolution call sites. Regression: `test_s52_consuming_hypothesis_host_restores_placeholder` (`inference_tests.rs`) — verified red without the fix. Full suite green (1314/1314) with zero regressions to the ~20 existing Illusion tests.

    **Still open, and this time on the NON-Illusion side**: a fresh sweep after the fix still reproduced `Not([Leftovers, LifeOrb]) excludes the revealed value Leftovers`, but this instance's `mon#1` was on **P1's side** (`MA_dragonite_rain.txt`), which has **no Illusion-capable roster member at all** in this matchup — ruling out S50/S52b's mechanism entirely for this specific occurrence and confirming the ORIGINAL absence-inference hypothesis (a singleton-item-clause collapse forcing `Known`/`Not` too early — see the singleton-collapse sites already identified: `pass_eot_heal`, `pass_eot_sand_immunity`, `pass2_powder_immunity`, `pass2_guaranteed_status_absence`) is ALSO a real, independent, unfixed cause, co-occurring with the roster-duplication one this session closed. Comparative sweep evidence (4/6 baseline vs 6/6 with-fix failures at 500 iterations, small-N and not conclusive either way) suggests this non-Illusion absence-inference cause may now be the DOMINANT remaining trigger. Next step: instrument `enforce_unique_item`'s exclusion call (dumping `source_idx`/target idx/species/item-before, as done for S52b) specifically filtered to non-Illusion-team pairings, and re-sweep.
  - ~~**A third status-conflict pair**~~ **FIXED (S51)** (Poison↔Paralysis, Poison↔Sleep, Burn↔Paralysis, Burn↔Frozen all observed) — root cause: `simulator::helpers::clear_pokemon_on_faint` clears `status`/`volatiles`/`times_hit` on the real engine's `PokemonState` the instant a mon faints, with **no matching event emission** (fainting is itself the observable signal — there is no separate "status cured by fainting" event in the vocabulary). The inference engine's `EventKind::Faint` handler (and its belt-and-braces `DamageDealt`-to-0 companion) only ever set `fainted`/`hp`, never clearing `status`/`volatiles`/`times_hit` — so a mon that entered with a status (a legitimate ground-truth reveal via the `Switch` event's own `status` field) and then fainted kept that stale status in the belief indefinitely. This also explains why the prior per-`mon_idx` event trace found "no switch or status event of any kind recorded" before the panic — that trace's filter never included `Faint`. Fix: both handlers now clear `status`/`volatiles`/`times_hit` to mirror `clear_pokemon_on_faint` exactly. Regressions: `test_s51_faint_clears_stale_status_before_slot_reuse`, `test_s51_faint_clears_stale_frozen_status` (`inference_tests.rs`) — both verified red without the fix. Full suite green (1312/1312); a fresh sweep (6×150 + 3×500 = ~2400 clean battles, one further 500-run hit the unrelated, already-open item-Known-conflict family below, not a status conflict) found zero recurrences of this family.
  - ~~**Doubles same-species Illusion-disguise collision, cross-slot case**~~ **FIXED (S50)** — `pass1_switch` now has a dedicated cross-slot guard: when the incoming species is already shown active in a DIFFERENT slot on the same side (Species Clause proves this can only be the side's disguised Illusion forme, never a second real individual), it routes the arrival through the parallel-hypothesis model directly — building the shown identity and consuming (removing, not just cloning) the side's Illusion-forme baseline from the bench into the arrival's `possible_illusion_state`, instead of falling into the old bench-match/rebuild path that either wrongly consumed a real bench member or fabricated a brand-new `Known(species)` phantom roster entry while the true Zoroark baseline sat untouched on the bench, double-counting one physical individual. Also hardened `restore_discarded_primary_to_bench` with a defensive check against ever pushing a bench duplicate of a species that's genuinely active elsewhere on the side. Regression: `test_s50_cross_slot_illusion_collision_does_not_duplicate_roster` (`inference_tests.rs`) — verified red without the fix (reproduces the exact "Zoroark baseline left on bench" defect), green with it. Full suite green (1310/1310); 5×25-iteration fresh sweep clean.
### New features
- Link all the READMEs to the main project README.
- Frontend features
  - We need a favicon lul
- Eventually create nash solver and recursive evaluation (When both players have perfect information)
- Create a meta sampler from pikalytics, and then get the algorithm to understand that
  - [ ] Tracker page: needs a parser for lines of input -> action / reaction tree
        (figuring out what is a reaction to what and what causes what from just the
        lines, also must add guaranteed effects so the user doesn't need to put those
        in manually). This will likely include some RegEx stuff as well. Need a
        detailed spec.
    - https://stitch.withgoogle.com/projects/6512361286860616575 for designs
    - https://github.com/PokeAPI/sprites for FE sprites (fetched at runtime, never committed)
  - [ ] Then move on to actual bot creation, battle and mentor pages.
